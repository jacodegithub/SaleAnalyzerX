import React from 'react';

const PieChart = ({ pieChart }) => {
  return (
    <div>
      <h2 className="text-2xl font-bold mb-4">Pie Chart</h2>
      {/* Render pie chart data as needed */}
    </div>
  );
};

export default PieChart;
